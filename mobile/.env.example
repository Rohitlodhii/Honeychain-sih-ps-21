# Deployed backend (default for all devices).
EXPO_PUBLIC_API_URL=http://3.109.3.187:8001
EXPO_PUBLIC_VERIFY_URL=https://beelink21.vercel.app
# Local dev overrides (copy to .env.local and uncomment one):
# Web / iOS simulator (same machine as backend):
# EXPO_PUBLIC_API_URL=http://localhost:8000
# Android emulator (host loopback):
# EXPO_PUBLIC_API_URL=http://10.0.2.2:8000
# Physical device (Expo Go) — use your PC's LAN IP:
# EXPO_PUBLIC_API_URL=http://192.168.1.5:8000
